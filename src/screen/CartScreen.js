import React from 'react';
import CartComponent from '../component/CartComponent';

const CartScreen = () => {
    return (
        <div>
            <CartComponent/>
        </div>
    );
};

export default CartScreen;